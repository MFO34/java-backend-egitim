# Üretim profili — minimal log, güvenli ayarlar
spring:
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: validate

logging:
  level:
    root: WARN
    com.eticaret: INFO

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
